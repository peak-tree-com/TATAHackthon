import 'package:flutter/material.dart';

const Color themeColor = Color.fromARGB(255, 13, 58, 255);
const Color homeColor = Color.fromARGB(255, 13, 146, 255);
Color warningColor =
      Colors.red.withOpacity(0.9);
Color themeColorWithOpacity =
    const Color.fromARGB(255, 21, 64, 253).withOpacity(0.9);

const int milliSeconds = 450;

Color blackWithOpacity = Colors.black.withOpacity(0.7);
